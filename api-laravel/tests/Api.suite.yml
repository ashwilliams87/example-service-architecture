actor: ApiTester
suite_namespace: Tests\Api
modules:
    enabled:
      - PhpBrowser:
          url: '%API_TESTS_REQUEST_BASE_URL%'
      - REST:
          depends: PhpBrowser
          url: '%API_TESTS_REQUEST_BASE_URL%'
    config:
      - Laravel:
          environment_file: .env.testing
